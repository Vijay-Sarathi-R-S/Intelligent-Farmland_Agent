services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: agritech-dev
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      PYTHONUNBUFFERED: "1"
      PORT: "5000"
    env_file:
      - .env
    volumes:
      - ./:/app
    command: python -m flask --app app run --host=0.0.0.0 --port=5000
